{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create venv (Windows)",
      "type": "shell",
      "command": "python -m venv .venv && .venv\\Scripts\\activate && python -m pip install -r requirements.txt",
      "problemMatcher": []
    },
    {
      "label": "Create venv (Mac/Linux)",
      "type": "shell",
      "command": "python3 -m venv .venv && source .venv/bin/activate && python -m pip install -r requirements.txt",
      "problemMatcher": []
    }
  ]
}
